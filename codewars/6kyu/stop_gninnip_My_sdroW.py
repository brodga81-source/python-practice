# Stop gninnipS My sdroW
# Codewars
# https://www.codewars.com/kata/5264d2b162488dc400000001/train/python

def spin_words(sentence):
'''
Задача:
Перевернуть все слова длиной пять и более символов, сохранив остальные слова без изменений.

Решение:
Разделил исходную строку на список слов с помощью split().
Прошёл циклом по каждому слову.
Проверил длину слова с помощью len().
Слова длиной пять и более символов перевернул срезом [::-1].
Добавил обработанные слова в результирующий список.
Объединил список обратно в строку через пробел с помощью join().

Что закрепил:
- метод split();
- перебор списка циклом for;
- условную обработку элементов;
- переворот строки срезом [::-1];
- метод append();
- сборку строки с помощью join().
'''
    string = sentence.split()
    result = []
    for word in string:
        if len(word) >= 5:
            result.append(word[::-1])
        else:
            result.append(word)
    return ' '.join(result)
