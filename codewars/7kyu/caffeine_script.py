# Caffeine Script
# Codewars
# https://www.codewars.com/kata/5434283682b0fdb0420000e6/train/python

def caffeine_buzz(n):
"""
Задача:
Вернуть строку в зависимости от делимости числа:
- на 3 -> "Java";
- на 3 и 4 -> "Coffee";
- если число четное, добавить "Script";
- иначе вернуть "mocha_missing!".

Решение:
Сначала обработал случай, когда число не делится на 3.
Затем проверил более специфичный случай делимости на 4,
после чего обработал оставшиеся варианты.

Что изучил:
- порядок проверки условий;
- принцип "от более специфичного к более общему";
- упрощение логики за счет уже известных условий;
- рефакторинг без изменения поведения программы.
"""
    if n % 3 != 0:
        return "mocha_missing!"
    if n % 4 == 0:
        if n % 2 == 0:
            return "CoffeeScript"
        return "Coffee"
    if n % 2 == 0:
        return "JavaScript"
    return "Java"
