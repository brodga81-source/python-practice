# Coding 3min
# Codewars
# https://www.codewars.com/kata/5714803d2817ffce17000a35/train/python

def find_a_b(numbers, c):
"""
Задача:
Найти первую пару чисел в отсортированном массиве,
произведение которых равно заданному числу.

Решение:
Использовал два вложенных цикла по индексам.
Внутренний цикл начинается с i + 1, чтобы не
использовать один и тот же элемент дважды и не
проверять одинаковые пары повторно.

Что изучил:
- разницу между индексом и значением;
- использование range(len(...));
- получение элемента по индексу (numbers[i]);
- начало внутреннего цикла с i + 1;
- возврат результата сразу после нахождения первой пары.
"""
    for y in range(len(numbers)):
        for x in range(y + 1, len(numbers)):
            if numbers[y] * numbers[x] == c:
                return [numbers[y], numbers[x]]
    return None
