# Square Every Digit
# Codewars
# https://www.codewars.com/kata/546e2562b03326a88e000020/train/python

def square_digits(num):
'''
Задача:
Возвести в квадрат каждую цифру числа, объединить результаты
и вернуть итоговое число.

Решение:
Преобразовал число в строку, чтобы пройтись по каждой цифре.
Каждый символ преобразовывал в число, возводил в квадрат,
снова преобразовывал в строку и добавлял к результату.
В конце преобразовал итоговую строку обратно в число.

Что изучил:
- преобразование типов (int ↔ str);
- работу со строковым представлением числа;
- постепенное построение строки через конкатенацию;
- анализ ошибок при работе с несовместимыми типами данных.
'''
    result = ''
    for ch in str(num):
        result += str(int(ch) ** 2)
    return int(result)
