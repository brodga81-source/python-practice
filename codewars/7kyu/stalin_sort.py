# Stalin Sort
# Codewars
# https://www.codewars.com/kata/699af631058f5c12b04f4efe/train/python

def stalin_sort(arr):
Задача:
Реализовать "Сталинскую сортировку": оставить только элементы,
которые не нарушают неубывающий порядок, изменяя исходный список.

Решение:
Создал новый список с первым элементом.
Последовательно сравнивал каждый следующий элемент
с последним сохранённым и добавлял только подходящие.
В конце заменил содержимое исходного списка через срез.

Что изучил:
- доступ к последнему элементу списка ([-1]);
- изменение списка "на месте" через arr[:];
- работу с range(1, len(...));
- анализ сообщений об ошибках тестов и исправление решения.
    if arr == []:
        return
    result = [arr[0]]
    for i in range(1, len(arr)):
        if result[-1] <= arr[i]:
            result.append(arr[i])
    arr[:] = result
    return 
